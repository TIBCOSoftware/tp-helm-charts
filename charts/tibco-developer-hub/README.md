# TIBCO Hub Helm Chart

A Helm chart for deploying a TIBCO Hub application

This chart is heavily based on the base Backstage chart.

See the [Backstage chart values section](https://github.com/backstage/charts/tree/main/charts/backstage) for details about the configuration.
