#
#
# Copyright © 2023 - 2026. Cloud Software Group, Inc.
# This file is subject to the license terms contained
# in the license file that is distributed with this file.
#

{{ if .Values.opensearchIngress.route.enabled -}}
apiVersion: route.openshift.io/v1
kind: Route
metadata:
  name: {{ .Release.Name }}-opensearch
  namespace: {{ .Release.Namespace }}
  {{- with .Values.opensearchIngress.route.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
spec:
  host: {{ .Values.opensearchIngress.route.host }}.{{ .Values.domain }}
  to:
    kind: Service
    name: {{ .Values.opensearch.clusterName }}-master
  tls:
    termination: passthrough
    insecureEdgeTerminationPolicy: Redirect
{{- end -}}
