{{- if or (eq (include "tp-cp-finops-otel-collector-on-prem.serviceEnabled" .) "true") (.Values.ingress.enabled) -}}
apiVersion: v1
kind: Service
metadata:
  name: {{ include "tp-cp-finops-otel-collector-on-prem.fullname" . }}
  namespace: {{ template "tp-cp-finops-otel-collector-on-prem.namespace" . }}
  labels:
    {{- include "tp-cp-finops-otel-collector-on-prem.labels" . | nindent 4 }}
    {{- include "tp-cp-finops-otel-collector-on-prem.component" . | nindent 4 }}
  {{- if .Values.service.annotations }}
  annotations:
    {{- range $key, $value := .Values.service.annotations }}
    {{- printf "%s: %s" $key (tpl $value $ | quote) | nindent 4 }}
    {{- end }}
  {{- end }}
spec:
  type: {{ .Values.service.type }}
  {{- if .Values.service.clusterIP }}
  clusterIP: {{ .Values.service.clusterIP }}
  {{- end }}
  {{- if and .Values.service.loadBalancerIP (eq .Values.service.type "LoadBalancer") }}
  loadBalancerIP: {{ .Values.service.loadBalancerIP }}
  {{- end }}
  {{- if and .Values.service.loadBalancerSourceRanges (eq .Values.service.type "LoadBalancer") }}
  loadBalancerSourceRanges:
    {{- range .Values.service.loadBalancerSourceRanges }}
    - {{ . }}
    {{- end }}
  {{- end }}
  {{- $ports := include "tp-cp-finops-otel-collector-on-prem.servicePortsConfig" . }}
  {{- if $ports }}
  ports:
    {{- $ports | nindent 4}}
  {{- end }}
  selector:
    {{- include "tp-cp-finops-otel-collector-on-prem.selectorLabels" . | nindent 4 }}
    {{- include "tp-cp-finops-otel-collector-on-prem.component" . | nindent 4 }}
  internalTrafficPolicy: {{ include "tp-cp-finops-otel-collector-on-prem.serviceInternalTrafficPolicy" . }}
  {{- if and (eq .Values.service.type "LoadBalancer") (.Values.service.externalTrafficPolicy) }}
  externalTrafficPolicy: {{ .Values.service.externalTrafficPolicy }}
  {{- end }}
{{- end }}
