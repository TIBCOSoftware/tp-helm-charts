{{- if .Values.ingress.enabled -}}
{{- $ingresses := prepend .Values.ingress.additionalIngresses .Values.ingress -}}
{{- range $ingresses }}
apiVersion: "networking.k8s.io/v1"
kind: Ingress
metadata:
  {{- if .name }}
  name: {{ printf "%s-%s" (include "tp-cp-finops-otel-collector-on-prem.fullname" $) .name }}
  {{- else }}
  name: {{ include "tp-cp-finops-otel-collector-on-prem.fullname" $ }}
  {{- end }}
  namespace: {{ template "tp-cp-finops-otel-collector-on-prem.namespace" $ }}
  labels:
    {{- include "tp-cp-finops-otel-collector-on-prem.labels" $ | nindent 4 }}
    {{- include "tp-cp-finops-otel-collector-on-prem.component" $ | nindent 4 }}
  {{- if .annotations }}
  annotations:
    {{- range $key, $value := .annotations }}
    {{- printf "%s: %s" $key (tpl $value $ | quote) | nindent 4 }}
    {{- end }}
  {{- end }}
spec:
  {{- if .ingressClassName }}
  ingressClassName: {{ .ingressClassName }}
  {{- end -}}
  {{- if .tls }}
  tls:
    {{- range .tls }}
    - hosts:
        {{- range .hosts }}
        - {{ tpl . $ | quote }}
        {{- end }}
      {{- with .secretName }}
      secretName: {{ . }}
      {{- end }}
    {{- end }}
  {{- end }}
  rules:
    {{- range .hosts }}
    - host: {{ tpl .host $ | quote }}
      http:
        paths:
          {{- range .paths }}
          - path: {{ .path }}
            pathType: {{ .pathType }}
            backend:
              service:
                name: {{ include "tp-cp-finops-otel-collector-on-prem.fullname" $ }}
                port:
                  number: {{ .port }}
          {{- end }}
    {{- end }}
---
{{- end }}
{{- end }}
